import React from 'react';

const BusinessCard = ({ title }) => {

    return (
        <section>
            <ul>
                <li>{title}</li>
            </ul>
        </section>
    );
};

export default BusinessCard;