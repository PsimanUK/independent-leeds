import React, { Component } from 'react';

class Map extends Component {
    render() {
        return (
            <section>
                <p>This is where the map will go!</p>
            </section>
        );
    }
}

export default Map;